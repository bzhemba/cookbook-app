<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/public/gallery-style.css"/>
    {{> header}}
    {{> scripts}}
</head>
<body>
<div class="container">
    <h2 class="heading-text">Find your <span>inspiration</span></h2>
    <div class="image-gallery">
        {{#each data.columns}}
            <div class="column">
                {{#each this.images}}
                    <div class="image-item">
                        <img src="{{src}}" alt="{{title}}">
                        <h3>{{title}}</h3>
                    </div>
                {{/each}}
            </div>
        {{/each}}
    </div>
</div>

</body>
</html>
